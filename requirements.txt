# gaston_rag — dependencias con versiones fijas
# Última revisión: Mayo 2026

# ── API ──────────────────────────────────────
fastapi==0.115.12
uvicorn==0.34.0

# ── RAG / LangChain ──────────────────────────
langchain==0.3.22
langchain-chroma==0.2.2
langchain-ollama==0.2.3
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.7

# ── Base vectorial ────────────────────────────
chromadb==0.6.3

# ── Conectores externos ───────────────────────
requests==2.32.3
huggingface-hub==0.30.2

# ── Seguridad / Rate limiting ─────────────────
slowapi==0.1.9

# ── Configuración ─────────────────────────────
python-dotenv==1.1.0

# ── Testing ───────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.24.0
httpx==0.27.0

# ── Configuración ─────────────────────────────
python-dotenv==1.1.0
# ── Dashboard ────────────────────────────────────────────
python-multipart==0.0.20    # requerido por HTMX form posts
jinja2==3.1.6               # templates HTML (ya incluido via FastAPI)